import {
  TableContainer as UITableContainer,
  TableContainerProps as UITableContainerProps,
} from "@chakra-ui/react";

const TableContainer = (props: UITableContainerProps) => {
  return <UITableContainer {...props} />;
};

export default TableContainer;

import {
  ModalHeader as UIModalHeader,
  ModalHeaderProps as UIModalHeaderProps,
} from "@chakra-ui/react";

const ModalHeader = (props: UIModalHeaderProps) => {
  return <UIModalHeader {...props} />;
};

export default ModalHeader;

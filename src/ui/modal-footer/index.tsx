import {
  ModalFooter as UIModalFooter,
  ModalFooterProps as UIModalFooterProps,
} from "@chakra-ui/react";

const ModalFooter = (props: UIModalFooterProps) => {
  return <UIModalFooter {...props} />;
};

export default ModalFooter;

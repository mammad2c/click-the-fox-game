import {
  ModalBody as UIModalBody,
  ModalBodyProps as UIModalBodyProps,
} from "@chakra-ui/react";

const ModalBody = (props: UIModalBodyProps) => {
  return <UIModalBody {...props} />;
};

export default ModalBody;

import {
  ModalOverlay as UIModalOverlay,
  ModalOverlayProps as UIModalOverlayProps,
} from "@chakra-ui/react";

const ModalOverlay = (props: UIModalOverlayProps) => {
  return <UIModalOverlay {...props} />;
};

export default ModalOverlay;

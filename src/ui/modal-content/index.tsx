import {
  ModalContent as UIModalContent,
  ModalContentProps as UIModalContentProps,
} from "@chakra-ui/react";

const ModalContent = (props: UIModalContentProps) => {
  return <UIModalContent {...props} />;
};

export default ModalContent;
